# Azure Event Grid MQTT Broker Configuration
# Copy this file to .env and fill in your Azure credentials

# Azure Event Grid MQTT Namespace hostname (without https://)
# Format: <namespace-name>.<region>.ts.eventgrid.azure.net
EVENTGRID_MQTT_HOSTNAME=<your-namespace>.westus2.ts.eventgrid.azure.net

# MQTT port (default: 8883 for secure MQTT)
EVENTGRID_MQTT_PORT=8883

# MQTT topic to publish to Event Grid
EVENTGRID_MQTT_TOPIC=sensor/data

# Publishing interval in seconds (for test client)
PUBLISH_INTERVAL=5

# MQTT Client ID for proxy (must be registered in Event Grid namespace)
MQTT_CLIENT_ID=mqtt_proxy

# Authentication Option 1: SAS Token Authentication
# Generate SAS token from Azure Portal or CLI
MQTT_USERNAME=<your-client-id>
MQTT_PASSWORD=<your-sas-token>

# Authentication Option 2: Certificate-based Authentication
# Paths to certificate files (inside container: /app/certs/)
# MQTT_CERT_FILE=/app/certs/client-cert.pem
# MQTT_KEY_FILE=/app/certs/client-key.pem
# MQTT_CA_CERTS=/app/certs/ca-cert.pem

# Local certificates directory (will be mounted to /app/certs in container)
# CERTS_DIR=./certs
